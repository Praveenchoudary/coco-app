apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: coco-cert
spec:
  secretName: coco-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
  - coco.praveen.info
  
